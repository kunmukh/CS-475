#include <iostream>
#include <string>
#include <stack>
#include <queue>

int main()
{
  using namespace std;
  string phrase;
  stack<char> stk;
  queue<char> que;

  cout << "Enter a phrase: ";
  getline(cin, phrase);

  for (int i = 0; i < phrase.length(); i++)
    {
      //put the letter at index i into stack and queue
      char ch = plrase[i];
      stk.push(ch);
      que.push(ch);
    }

  bool palindrome = true;
